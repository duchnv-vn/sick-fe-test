export const DEFAULT_DEVICE_STATUS = [0, 1];

export const DEFAULT_DEVICE_TYPE = 'ALL';

export const deviceStatusLabels = {
  0: 'Offline devices',
  1: 'Online devices',
} as Record<number, string>;
